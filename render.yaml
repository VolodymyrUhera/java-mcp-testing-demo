services:
  - type: web
    name: 90s-MCP
    env: docker
    plan: free
    healthCheckPath: /
    autoDeploy: true
    envVars:
      - key: PORT
        value: 8080
      - key: RETRO90S_MCP_URL
        value: http://localhost:8081/message
